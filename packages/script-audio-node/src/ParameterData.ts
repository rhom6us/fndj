import { ARG1, ARG2, ARG3, ARG4 } from './constants';

// await audioContext.value.audioWorklet.addModule(url);
export interface ParameterData {
  [ARG1]?: number;
  [ARG2]?: number;
  [ARG3]?: number;
  [ARG4]?: number;
}
